# Book settings
# Learn more at https://jupyterbook.org/customize/config.html

title: Tales of Dry Lands
author: George Begkas, Marina Caporlingua, Lucy Colley, Akis Karagiannis - Thanks to ECMWF mentors Christopher Barnard, Francesca Di Giuseppe and Jessica Keune
logo: ../images/logo-no-bg.png

# Force re-execution of notebooks on each build.
# See https://jupyterbook.org/content/execute.html
execute:
  execute_notebooks: force

# Define the name of the latex output file for PDF builds
latex:
  latex_documents:
    targetname: book.tex

# Add a bibtex file so that we can create citations
bibtex_bibfiles:
  - references.bib

# Information about where the book exists on the web
repository:
  url: https://github.com/ECMWFCode4Earth/tales-of-drought/handbook  # Online location of your book
  path_to_book: docs  # Optional path to your book, relative to the repository root
  branch: master  # Which branch of the repository should be used when creating links (optional)

# Add GitHub buttons to your book
# See https://jupyterbook.org/customize/config.html#add-a-link-to-your-repository
# https://jupyterbook.org/en/stable/advanced/html.html
html:
  use_issues_button: true
  use_repository_button: true

sphinx:
  config:
    html_css_files: ["custom.css"]
    html_show_copyright: false
    language: en
    html_js_files:
    - https://cdnjs.cloudflare.com/ajax/libs/require.js/2.3.4/require.min.js
    suppress_warnings: ["mystnb.unknown_mime_type"]
    
execute:
  timeout: 500
